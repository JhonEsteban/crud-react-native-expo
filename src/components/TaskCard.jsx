import { StyleSheet, Text, View } from 'react-native';

const TaskCard = ({ task }) => {
  return (
    <View>
      <Text>TaskCard</Text>
    </View>
  );
};

export default TaskCard;

const styles = StyleSheet.create({});
